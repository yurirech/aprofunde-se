import React from 'react';

const ItineraryTable = ({title, }) =>
     (
      <div className="itinerary-table">
        {title ? <h1>{title}</h1> : null}
        {}
      </div>
    );

export default ItineraryTable;
